const form=document.querySelector('#login-form');const error=document.querySelector('#error');const submit=document.querySelector('#submit');
async function alreadySignedIn(){try{const response=await fetch('/api/owner-auth',{cache:'no-store'});if(response.ok)location.replace('/owner/monitor')}catch{}}
form.addEventListener('submit',async(event)=>{event.preventDefault();error.textContent='';submit.disabled=true;submit.textContent='Signing in…';try{const response=await fetch('/api/owner-auth',{method:'POST',headers:{'content-type':'application/json'},credentials:'same-origin',body:JSON.stringify({email:document.querySelector('#email').value,password:document.querySelector('#password').value})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Sign-in failed.');location.replace('/owner/monitor')}catch(reason){error.textContent=reason.message||'Sign-in failed.';submit.disabled=false;submit.textContent='Sign in securely'}});alreadySignedIn();
